package features.in.java7;

public class SimplifiedVarargs {
	/**
	 * When a programmer tries to invoke a *varargs* (variable arity) method
	 * with a non-reifiable varargs type, the compiler currently generates an
	 * “unsafe operation” warning. JDK 7 moves the warning from the call site to
	 * the method declaration. This will enable API designers to use varargs due
	 * to the reduction of warnings reported.
	 */

}
